# Continuous Integration

The executable CI definition lives in `.github/workflows/ci.yml`, where GitHub Actions expects it.
This directory remains the blueprint-aligned home for supporting CI scripts if they become necessary.
